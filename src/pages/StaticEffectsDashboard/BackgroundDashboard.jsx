import { Box } from "@mui/material"

export default function BackgroundDashboard({ elementStyles }) {
    return (
        <Box>
            <Box>
                Fill Color + Opacity
            </Box>
        </Box>
    );
}